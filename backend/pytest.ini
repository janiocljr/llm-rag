[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
markers =
    unit: fast unit tests (no model loading)
    integration: tests that require model files
    eval: evaluation tests that require eval_dataset.json
addopts = -ra -q
